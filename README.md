# team03
바보 대회 팀3의 키보드 프로젝트입니다.

## 실행 방법

`cursed-chat.jsx`는 Vite + React + Tailwind CSS 프로젝트로 세팅되어 있고,
그 내용은 `src/App.jsx`에도 동일하게 복사되어 있습니다.

```bash
npm install   # 처음 한 번만
npm run dev
```

명령 실행 후 터미널에 나오는 주소(기본 `http://localhost:5173`)를 브라우저로 열면 됩니다.

종료는 `Ctrl+C`.
